profiles {
  default {
    params {
      outputDir = "$launchDir/output"
    }
    process {
      container = 'veupathdb/site-search-data:1.2.0'
      maxForks = 4
    }
    docker {
      enabled = true
    }
  }
}
